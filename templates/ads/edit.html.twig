{% extends 'base.html.twig' %}

{% block title %}Edit Ads{% endblock %}

{% block body %}
    <h1>Edit Ads</h1>

    {{ include('ads/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ads_index') }}">back to list</a>

    {{ include('ads/_delete_form.html.twig') }}
{% endblock %}
