<form method="post" action="{{ path('app_reclamation_avis_delete', {'id': reclamation_avi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation_avi.id) }}">
    <button class="btn">Delete</button>
</form>
