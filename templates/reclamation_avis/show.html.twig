{% extends 'base.html.twig' %}

{% block title %}ReclamationAvis{% endblock %}

{% block body %}
    <h1>ReclamationAvis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation_avi.id }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ reclamation_avi.objet }}</td>
            </tr>
            <tr>
                <th>Dateajout</th>
                <td>{{ reclamation_avi.dateajout ? reclamation_avi.dateajout|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation_avi.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ reclamation_avi.image }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ reclamation_avi.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_avis_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_avis_edit', {'id': reclamation_avi.id}) }}">edit</a>

    {{ include('reclamation_avis/_delete_form.html.twig') }}
{% endblock %}
