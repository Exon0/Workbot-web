{% extends 'utilisateur/Dashbord/base.html.twig' %}

{% block body %}
    <section class="section">
    <div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 70px;">
    <div class="col-lg-6">
    <div class="card">
    <div class="card-body" >
    <div style="margin-left: 50px">
        <br>
        <h6 class="card-title" style="display: flex;font-size: 30px;justify-content: center;color: black " >Teste de Certification </h6>
        {{ form_start(form) }}
        <form class="col-xxl-8 col-sm-4" style="margin-left: 30px" >
            {% block choice_widget %}
                <h5>{{ form_label(form.question1) }}</h5>
                <br>
                {% for child in form.question1 %}

                    <div class="currency-label">
                        {{- form_widget(child,{'attr':{'class':'form-check-input'}}) -}}
                        <a class="form-check-label"> {{- form_label(child, null) -}}</a>
                    </div>

                {% endfor %}
                <br>

                <h5> {{ form_label(form.question2) }}</h5>
                <br>
                {% for child in form.question2 %}
                    <div class="currency-label">
                        {{- form_widget(child,{'attr':{'class':'form-check-input'}}) -}}
                        <a class="form-check-label"> {{- form_label(child, null) -}}</a>
                    </div>
                {% endfor %}
                <br>

              <h5>{{ form_label(form.question3) }}</h5>
                <br>

                {% for child in form.question3 %}
                    <div class="currency-label">
                        {{- form_widget(child,{'attr':{'class':'form-check-input'}}) -}}
                        <a class="form-check-label"> {{- form_label(child, null) -}}</a>
                    </div>
                {% endfor %}
                <br>

                <h5>  {{ form_label(form.question4) }}</h5>
                <br>
                {% for child in form.question4%}
                    <div class="currency-label">

                        {{- form_widget(child,{'attr':{'class':'form-check-input'}}) -}}
                        <a class="form-check-label"> {{- form_label(child, null) -}}</a>
                    </div>
                {% endfor %}
            {% endblock choice_widget %}
            <br>
            <center> {{ form_widget(form.Valider,{'attr':{'class':'btn btn-primary'}}) }}</center>
        </div>
        {{ form_end(form) }}<!-- End General Form Elements -->
        <br>
    </div>
    </div>
    </div>
    </div>
    </div>
    </section>

{% endblock %}